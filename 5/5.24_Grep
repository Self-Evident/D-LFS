#!/bin/bash
######################################################################
#
PACKAGE_NAME="grep-2.9"
PACKAGE_FILE="${PACKAGE_NAME}.tar.gz"
PACKAGE_DESC=""
SBUS="0.2"

source D-LFS.functions

header_log_prep

./configure --prefix=/tools \
    --disable-perl-regexp  &>> $BUILD_LOG 

make           &>> $BUILD_LOG
make install   &>> $BUILD_LOG

cleanup_log_footer
