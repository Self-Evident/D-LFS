
######################################################################
#
PACKAGE_NAME="udev-lfs-188"
PACKAGE_FILE="systemd-188.tar.xz"
PACKAGE_FILE2="udev-lfs-188-3.tar.bz2"
PACKAGE_DESC=""
SBUS="0.1"  #0.1

source D-LFS.functions 

header_log_prep

tar -xvf ../${PACKAGE_FILE2}

make -f     ${PACKAGE_NAME}/Makefile.lfs

make -f     ${PACKAGE_NAME}/Makefile.lfs install

bash        ${PACKAGE_NAME}/init-net-rules.sh

cleanup_log_footer
