
######################################################################
#
PACKAGE_NAME="automake-1.11.1"
PACKAGE_FILE="${PACKAGE_NAME}.tar.bz2"
PACKAGE_DESC=""
SBUS="0.0"  #18.3 including test suites

source D-LFS.functions 

header_log_prep

./configure --prefix=/usr --docdir=/usr/share/doc/$PACKAGE_NAME &>> $BUILD_LOG 
make                        &>> $BUILD_LOG
# make check                  &>> $BUILD_LOG
make install                &>> $BUILD_LOG

cleanup_log_footer
